package frame.example;

import javax.swing.*;
import java.awt.*;

/**
 *  // JFrame  = a GUI window to add components to
 */
public class MyFrame extends JFrame {
    //constructor is called whenever we make an instance of MyFrame
    /*
    MyFrame() {
        this.setTitle("JFrame title goes here"); //sets title of frame
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of the application
        this.setSize(420, 420); //sets the x-dimension, and y-dimension of frame
        this.setVisible(true); //make frame visible
        // this.setResizable(false); //prevent frame from being resized

        //create an instance of ImageIcon
        ImageIcon image = new ImageIcon("/Users/zohrehashtarilarki/Desktop/My Codes(49J)/GUI/GUI/logo.png");
        this.setIconImage(image.getImage()); //change icon of frame
        //this.getContentPane().setBackground(Color.blue); //change color of background
        //creating custom background color
        this.getContentPane().setBackground(new Color(123, 50, 250)); //RGB values from 0-255
        //this.getContentPane().setBackground(new Color(0x123456)); //hexadecimal color value from 0x000000-0xFFFFFF
    }
    */

}
